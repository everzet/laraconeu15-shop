default:
    suites:
        domain:
            contexts: [ BasketContext ]
        gui:
            contexts: [ BasketGUIContext ]
            filters:  { tags: @critical }

    extensions:
        Behat\MinkExtension:
            base_url: http://localhost:8000
            sessions:
                default: { goutte: ~ }
